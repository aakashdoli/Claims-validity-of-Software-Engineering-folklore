# LLM prototypes (not used in thesis instrument)

These scripts are early prototypes that use an LLM to extract claims directly from raw text/PDF.

They are not used in the thesis instrument because the thesis requires:
- a reproducible, deterministic extraction instrument
- auditable traceability
- validation through manual sampling

The thesis instrument lives in `src/se_claims_tool/`.